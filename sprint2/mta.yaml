_schema-version: "3.2"
ID: appsprint2
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appsprint2-destination-content
  type: com.sap.application.content
  requires:
  - name: appsprint2-destination-service
    parameters:
      content-target: true
  - name: appsprint2-repo-host
    parameters:
      service-key:
        name: appsprint2-repo-host-key
  - name: appsprint2-uaa
    parameters:
      service-key:
        name: appsprint2-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appsprint2_html_repo_host
          ServiceInstanceName: appsprint2-html5-service
          ServiceKeyName: appsprint2-repo-host-key
          sap.cloud.service: appsprint2
        - Authentication: OAuth2UserTokenExchange
          Name: appsprint2_uaa
          ServiceInstanceName: appsprint2-xsuaa-service
          ServiceKeyName: appsprint2-uaa-key
          sap.cloud.service: appsprint2
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appsprint2-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appsprint2-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appsprint2.zip
      name: appsprint2
      target-path: resources/
- name: appsprint2
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appsprint2-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appsprint2-destination-service
    service-plan: lite
- name: appsprint2-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appsprint2-xsuaa-service
    service-plan: application
- name: appsprint2-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appsprint2-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
